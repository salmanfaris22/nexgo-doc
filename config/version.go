package info

const (
	Version     = "1.2.0"
	PrevVersion = "1.1.0"
	GoVersion   = "1.22+"

	InstallCmd     = "go install github.com/salmanfaris22/nexgo/cmd/nexgo@v" + Version
	PrevInstallCmd = "go install github.com/salmanfaris22/nexgo/cmd/nexgo@v" + PrevVersion

	LatestBadge  = "v" + Version + " is live — Star us on GitHub"
	VersionBadge = "v" + Version
	PrevBadge    = "v" + PrevVersion
)
